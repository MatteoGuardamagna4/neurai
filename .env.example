# Copy to .env. Local LM Studio needs none of these.
OPENAI_API_KEY=            # only for a non-local tutor.base_url (config tutor.api_key_env)
NEUROTUTOR_SERVER_URL=     # printed by notebooks/serve_models.ipynb, for simulate --remote
NEUROTUTOR_SERVER_TOKEN=
HF_TOKEN=                  # only for local Hugging Face downloads; Colab uses its own secret
